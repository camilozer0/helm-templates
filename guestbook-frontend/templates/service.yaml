apiVersion: v1
kind: Service
metadata:
  name: {{ include "guestbook-frontend.fullname" . }}
  labels:
    app: {{ include "guestbook-frontend.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "guestbook-frontend.name" . }}
